__pycache__/
*.py[cod]
.venv/
.env
.env.*
!.env.example
.amplifier/
artifacts/
dist/
*.egg-info/
*.sqlite3*
*connection*.json
teamwork-overlay*
.DS_Store

# Evaluation run output: results belong outside the repo, per this project's
# convention (see evals/*/README.md). Never commit a run's session data,
# transcripts, or reports.
evals/*/results/
